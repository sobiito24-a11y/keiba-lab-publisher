# KEIBA LAB Publisher Ver.3 実装報告

## 1. 変更内容

- X API・OAuth・自動投稿・予約投稿を完全削除
- 🏢通常平日／🏃忙しい日／🌴休日の運用モード
- 保存済み材料だけによる「今日の公開候補」と手動1R選択
- URL込み280文字以内のX原稿、丸数字馬番、コピーボタン
- Ver.2の全レースnote品質、編集可能な固定冒頭文、主のひとことを維持
- 手動「X投稿済みにする」と公開記録・本文hash・二重記録防止
- 正式結果JSONだけを使う結果リプ、主の結果ひとこと、コピーボタン
- 今日のnote/X/結果進捗一覧
- state schema v3とVer.1/2移行

## 2. 変更ファイル

- `app.py`
- `publisher/content.py`
- `publisher/operations.py`（新規）
- `publisher/results.py`（新規）
- `publisher/state.py`
- `publisher/posting.py`（削除）
- `requirements.txt`
- `README.md`
- `tests/test_content.py`
- `tests/test_state.py`
- `tests/test_v2.py`（削除）
- `tests/test_v3.py`（新規）
- `scripts/audit_ooi_v2.py`（削除）
- `scripts/audit_publisher_v3.py`（新規）
- `examples/*_v3.*`

## 3. テスト結果

`49 passed`。Streamlitアプリのimport確認も成功しました。

## 4–6. 予想不変・実データ監査

添付 `2026-08-14_NAR_大井.keiba` の大井1R～10Rを監査しました。

- Prediction Snapshot変更：0件（生成前後署名一致）
- 予想再計算：0回
- noteレース数：10/10
- X印・馬名欠落：0件
- 内部ラベル残存：0件
- URL挿入：10/10
- X最大換算文字数：203/280
- 通常平日表示：10R
- 忙しい日・休日表示：選択1R
- 手動公開二重記録：全件拒否
- 正式結果取得済み：0/10
- 実データ結果表示：`結果データ未取得`

公開候補は大井1Rでした。理由は「◎が保存済み総合評価1位、◎が能力評価1位」で、保存済み事実だけを利用しています。

## 7. note完成例

### 能力1位＝◎：大井1R

能力比較で先頭に立つ7番ハルイチバンが◎。地力を素直に軸に据える構図だ。

7番ハルイチバンは総合評価1位。高指数、コース実績、展開材料を材料に今回の中心として評価した。

○8番ジュアルエリモは高指数、距離実績。▲6番ユウトザケンケンは対戦先着、高指数。◎との差だけでなく、展開ひとつで順序が替わる余地も見ておきたい。

### 能力1位≠◎：大井2R

能力値では8番フィールドアピールが最上位だが、今回条件まで含めた総合評価は16番ガーリッシュを◎に選んだ。能力順どおりではなく、適性と展開を比べたい一戦。

16番ガーリッシュは総合評価1位。対戦先着、高指数、距離実績を材料に今回の中心として評価した。

○4番ナムラソランは対戦劣勢、高指数。▲1番ヒガシナデシコは高指数、距離実績。◎との差だけでなく、展開ひとつで順序が替わる余地も見ておきたい。

### 妙味馬あり：大井3R

能力値では6番ビートリニティが最上位だが、今回条件まで含めた総合評価は3番ナックジャスパーを◎に選んだ。能力順どおりではなく、適性と展開を比べたい一戦。

3番ナックジャスパーは総合評価1位。高指数、距離実績、コース実績を材料に今回の中心として評価した。

○9番エムティキングは対戦◎、高指数。▲6番ビートリニティは対戦劣勢、高指数。◎との差だけでなく、展開ひとつで順序が替わる余地も見ておきたい。

妙味あり欄では、◎3番ナックジャスパーと○9番エムティキングを、買い推奨と断定せず市場評価との比較候補として掲載します。

## 8. X完成例

```text
【大井2R｜KEIBA LAB AI予想】

◎⑯ ガーリッシュ
○④ ナムラソラン
▲① ヒガシナデシコ
△③ リコーブルーアイ
☆⑫ カクヒメ

能力比較では8番フィールドアピールが上位も、今回条件を含め16番ガーリッシュを中心視。妙味では○4番ナムラソランにも注目。

🐴全レース予想・詳しいAI考察はnote👇
https://note.com/keiba_lab_ai

#大井2R #AI競馬予想
```

## 9. 結果リプ例

添付実データには正式結果がないため、実際の出力は `結果データ未取得` です。

正式結果JSONに「◎16番1着・△3番3着・ワイド410円」が明記されたテストでは次を生成します。

```text
◎16番 1着🏇
△3番 3着💥
ワイド 3-16 410円

ここはガミ、、、🐕笑
```

## 10. Ver.2 state互換性

生成/手動note、生成/手動X、冒頭文、主のひとこと、note URL、無料race_idを引き継ぎます。旧 `post_history` は `legacy_x_api_history` へ隔離します。API post ID・HTTPエラーを新しい手動公開記録へ変換しません。

## 11. 削除したX API関連

接続確認、認証、Key/Token、OAuth、dry-run、1投稿テスト、自動投稿、一括投稿、投稿間隔、予約監視、API成功/失敗履歴、`requests`、`requests-oauthlib` を削除しました。

## 12. 既知の制約

- Xとnoteへの公開は手動です。
- クリップボードはブラウザの権限設定により拒否される場合があります。その場合はテキスト欄からコピーできます。
- 結果リプは `.keiba` 内またはアップロードJSONに正式結果がある場合だけ生成します。
- 配当の的中判定や券種の推測はしません。正式データにある配当行を事実として表示します。
- Git commit / pushは実施していません。
